# Copy to .env and change the value before running the server.
# ADMIN_TOKEN protects GET /api/inbox and PATCH/DELETE /api/inbox/:id.
ADMIN_TOKEN=change-me-to-a-long-random-string

# Port for the standalone Node server (`npm run preview`). Optional.
PORT=4322
